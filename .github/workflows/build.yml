name: Build Release

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: BigWigsMods/packager@v2
        with:
          args: -d
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Rename zip and fix timestamps
        id: package
        run: |
          cd .release
          ORIG_ZIP=$(ls OnEventWatch-*.zip)
          mkdir -p _tmp
          cd _tmp
          unzip -o "../$ORIG_ZIP"
          VERSION=$(grep '## Version:' OnEventWatch/OnEventWatch.toc | sed 's/## Version: *//;s/\r//')
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

          find . -exec touch -t "$(date +%Y%m%d%H%M.%S)" {} +

          ZIPNAME="OnEventWatch-v${VERSION}-bcc.zip"
          zip -r "../$ZIPNAME" .
          cd ..
          rm -rf _tmp
          rm -f "$ORIG_ZIP"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: OnEventWatch v${{ steps.package.outputs.version }}-bcc
          body: |
            ## Download

            Download **OnEventWatch-v${{ steps.package.outputs.version }}-bcc.zip** below. Do not download the "Source code" archives.

            Extract the `OnEventWatch` folder into your WoW AddOns directory:
            ```
            World of Warcraft/_anniversary_/Interface/AddOns/OnEventWatch/
            ```

            Built from `${{ github.ref_name }}` at ${{ github.sha }}.
          files: .release/OnEventWatch-v${{ steps.package.outputs.version }}-bcc.zip
          fail_on_unmatched_files: true
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
